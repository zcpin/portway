# SSH Tunnel 扩展实施记录

起点：`71b0a97`。用户已授权按下列顺序实现，每项验收后单独提交，再继续下一项。

## 约束与兼容

- 保持 Go daemon + Flutter/Riverpod 桌面客户端架构；HTTP 和 WebSocket 继续只在本机使用并校验 token。
- 现有 TOML、REST 字段和 `status` 事件保持兼容；新字段提供兼容默认值。用户配置的写入继续走校验与原子保存。
- 私钥不复制到项目；私钥口令只在解锁请求中使用，解锁结果保存在 daemon 内存，不能写入 TOML、日志或导出文件。
- SDK/vfox 目录只读。测试缓存使用项目外的专用临时目录；不执行实际发布、卸载或替换当前安装。
- 每项先完成对应回归测试，再运行相关 Go/Flutter 检查；更新本记录并提交代码。

## 实施顺序与验收

| 项 | 方向 | 状态 | 验收范围 |
|---|---|---|---|
| 1 | 连接状态与诊断 | 已完成 | 连接中/已连接/重连中/失败/停止、最近错误、重试次数、连接时间；编辑器测试 SSH 连接；运行状态实时推送 |
| 2 | 多隧道管理 | 已完成 | 项目分组、搜索筛选、复制配置、批量启停、逐条结果；每条隧道的自动启动开关，旧配置默认自动启动 |
| 3 | 配置迁移与恢复 | 已完成 | TOML 导入导出、无副作用的校验和冲突预览、导入前备份、备份列表与恢复；失败保留原配置 |
| 4 | SSH 认证与主机校验体验 | 已完成 | ssh-agent、带口令私钥的会话解锁/锁定、主机指纹查看与确认；未知/变更指纹不会自动信任 |
| 5 | 更多转发模式 | 已完成 | 本地/反向 TCP 转发、SOCKS5 动态代理、引用 SSH 连接组成跳板链；保留现有本地转发默认行为 |
| 6 | 多实例与工作区 | 已完成 | 展示可用 daemon、选择并记住目标、添加独立配置工作区并启动；切换后重建连接和全部数据状态 |
| 7 | 版本检查与升级 | 已完成 | 稳定/预发布版本检查、匹配平台产物、下载校验、明确安装方式；支持可回滚的便携升级并保留配置 |

## 阶段设计

### 1. 连接状态与诊断

daemon 在隧道生命周期中记录结构化运行状态，保留 `is_running` 表示运行任务是否存活。列表快照附带详细状态，新增 `runtime` 事件同步连接阶段和错误变化。旧客户端仍收到布尔 `status`。客户端展示详细状态，重连不覆盖较新的推送。

测试连接使用待保存的 SSH 配置执行一次有超时的握手，不创建隧道、不保存配置、不修改主机信任。返回耗时和可读错误，并在请求取消后释放连接。

验收：失败重试、重试耗尽、连接成功、停止/重启的状态转换；旧运行不能覆盖新运行；测试连接不写盘且受认证保护；客户端模型、事件及操作回归。

### 2. 多隧道管理

配置增加分组与可缺省的自动启动字段。分组用于客户端筛选；批量动作由 daemon 返回每条隧道结果，允许部分成功并明确显示失败。复制配置生成新名称，保留认证、转发与重连选项。

验收：旧 TOML 行为不变、禁用自动启动的隧道仍可手动启动、热重载保留手动状态、批量异常不掩盖其他结果、复制/筛选不丢字段。

### 3. 配置迁移与恢复

增加完整配置的导出、预览、导入和备份恢复接口及界面。预览显示重名项和将被影响的隧道；确认导入时重新校验并校验当前配置版本，防止预览后的并发修改被覆盖。备份使用 daemon 配置目录下的专用目录和服务端生成的文件名。

验收：非法/超大配置、名称冲突、并发版本变化、备份/写盘失败、路径穿越均保持现有配置；恢复可重现原有字段和隧道运行策略。

### 4. SSH 认证与主机校验体验

认证方式默认为私钥文件，可选择系统 SSH agent。带口令私钥通过独立解锁动作加载到内存；重启 daemon 后重新解锁。指纹预览和信任写入分离，写入前重查指纹，变更指纹需要明确确认。

实现约定：`auth_method=key/agent`、可选 `agent_socket`；Windows 默认 OpenSSH 命名管道，Unix 使用 `SSH_AUTH_SOCK`。每个 Manager 持有独立内存密钥缓存，以文件摘要检测密钥变化。锁定只影响之后的认证，不中断现有连接。主机指纹确认前重新探测并比较指纹，替换记录时保留其他主机的信任关系。

验收：正确/错误口令、锁定后重新连接、agent 不可用、指纹首次确认/变更/并发变化；敏感内容不进入配置导出和日志。系统服务需要能访问其运行身份下的 agent。

### 5. 更多转发模式

新增转发类型，默认 `local`。反向转发由 SSH 远端监听、本机连接目标；动态代理仅支持 SOCKS5 CONNECT，目标连接由 SSH 服务端发起。跳板链引用已定义 SSH 连接，验证缺失引用与循环。

配置字段：`mode=local/remote/dynamic`、`local_host`（缺省 `127.0.0.1`）、`proxy_jump`（按顺序引用 SSH 连接名）。本地转发和动态代理的监听限制为回环地址；反向模式中 local 为本机目标、remote 为远端监听。跳板引用可嵌套展开，拒绝重复、循环或超过 8 跳的链。完整链共用连接超时；扫描目标指纹时先按配置认证跳板。

验收：三种模式的数据双向传输、半关闭、停止释放连接、SOCKS5 非法请求/域名/IPv6、跳板认证失败与取消清理；现有配置与转发测试继续通过。

### 6. 多实例与工作区

保留自动发现，增加显式选择。工作区具有独立配置及发现目录；创建时使用空配置，不借用其他实例的凭据。切换目标关闭旧客户端，并刷新隧道、连接、密钥、日志和设置。

验收：多个可用候选、失效 token、选定实例离线、daemon 重启、工作区路径与名称校验，以及切换期间迟到事件隔离。

### 7. 版本检查与升级

复用 GitHub Release 版本与平台产物。版本比较遵循语义版本并区分发布渠道，下载后验证对应 SHA256 文件。升级由运行目录外的帮助进程等待程序退出后执行，备份原安装并在替换/启动失败时回滚。用户配置留在原用户目录；不把系统服务安装当作普通便携目录自动覆盖。

验收：版本比较、渠道/平台筛选、下载错误与校验失败、压缩包路径穿越、等待退出超时、替换失败回滚；全部替换测试限定在临时夹具目录。

实现边界：客户端调用随包 daemon 的 `update` 子命令检查和下载，无需连接当前实例。发布时注入实际 GitHub 仓库并写入便携标识；安装器不携带便携标识。升级助手复制到用户缓存目录，使用同卷暂存、整目录备份和启动握手确认，失败恢复原目录。只允许有便携标识且配置位于安装目录外的安装自动替换；系统服务、未鉴权实例和其他安装目录不会被停止或覆盖。升级界面明确确认隧道中断，停止同一安装的用户实例后重启原配置。下载和升级结果保留在专用缓存目录供查看。

## 风险与检查点

- 运行状态会跨越异步拨号、重启和 WebSocket 重连，必须绑定到同一次运行并测试并发。
- 配置与运行实例必须一起保持一致；导入/恢复复用事务路径，不能直接覆盖文件后忽略加载错误。
- agent、主机信任与升级具有平台差异；公共逻辑用单元/集成测试，平台分支交由对应构建检查。
- 转发和跳板链会增加连接生命周期复杂度，继续使用统一取消、资源登记和半关闭规则。
- 本次不进行真实 SSH 账户测试、GitHub 发布或当前安装升级；使用本地 SSH 服务、模拟 API 和临时文件验证。

## 完成记录

每项完成后在此记录实现、测试结果和对应提交。

### 第 1 项：连接状态与诊断

- 新增结构化状态和兼容的 `runtime` 事件；运行代次检查防止旧连接修改新状态。
- 新增有认证、可取消的 SSH 测试接口；编辑器使用未保存的输入测试并展示耗时和错误。
- 验证通过：Go 全量测试、vet、Linux race；Dart analyze、Flutter 全量测试及新增编辑器/事件回归；测试覆盖成功握手、取消释放、状态转换和无副作用诊断。
- 提交：`8fdcbaa`（`feat: 增加隧道运行诊断与连接测试`）。

### 第 2 项：多隧道管理

- 新增分组/搜索、多选批量启停及逐项结果；复制时生成新名称和未被配置占用的端口。
- `auto_start` 缺省兼容原行为；显式关闭后可以手动启动，编辑元数据不影响当前连接。
- 验证通过：Go 全量测试、vet、Linux race；Dart analyze、Flutter 回归。覆盖开关持久化、快照指针隔离、热重载保持手动状态、部分失败/重复操作，以及筛选和复制的界面行为。
- 提交：`84eba35`（`feat: 增加隧道分组批量操作与自动启动策略`）。

### 第 3 项：配置迁移与恢复

- 设置页支持文件导入/导出、合并或替换预览、确认导入、备份列表与恢复预览；导入限制为 256 KiB。
- 修订号同时覆盖内存配置和磁盘内容，阻止过期预览覆盖并发修改；导入前保存原文件，备份读取通过限定目录和文件名验证。
- 验证通过：Go 全量测试、vet、Linux race；Dart analyze 和 Flutter 27 项测试。覆盖合并/恢复、未知字段、大小限制、版本冲突、写盘/备份失败、路径穿越、越界符号链接，以及界面的预览与确认约束。
- 提交：`bd5e0ab`（`feat: 增加配置导入导出与备份恢复`）。

### 第 4 项：SSH 认证与主机校验体验

- SSH 连接和直连隧道支持 agent；Windows 使用命名管道，Unix 使用 Unix socket。握手超时同时覆盖 agent 操作。
- 带口令私钥可会话解锁/锁定，缓存绑定文件摘要，密钥变化后失效；配置、响应和日志不包含口令。
- 主机指纹查看与信任分离，确认时重新比对；变更必须明确替换，保留同一行其他主机以及哈希主机记录的正确行为。
- 验证通过：Go 全量测试、vet、Linux race；Dart analyze 和 Flutter 32 项测试。使用本地 SSH/agent 夹具覆盖认证、取消、密钥变化、信任替换及界面确认。
- 提交：`f12b729`（`feat: 支持 SSH agent、私钥解锁与主机指纹确认`）。

### 第 5 项：更多转发模式

- 增加反向 TCP 转发和 SOCKS5 CONNECT（IPv4、IPv6、远端域名解析）；本地转发保留原行为。
- 跳板链可引用和展开 SSH 连接，统一超时与清理，支持经跳板检查目标主机指纹；界面区分监听与目标地址。
- 补齐空 agent 地址的回归，避免空路径被当作当前目录，确保使用系统默认 agent。
- 验证通过：Go 全量测试、vet、Linux race；Dart analyze 和 Flutter 35 项测试。真实本地 SSH 夹具验证两级跳板、反向转发、双向半关闭、指纹扫描及失败后释放；另覆盖 SOCKS5 协议和配置图校验。
- 提交：`95f9f2a`（`feat: 支持反向转发 SOCKS5 与多级跳板`）。

### 第 6 项：多实例与工作区

- 顶部实例选择器显示连接状态，记住选择；新工作区使用随机标识的独立目录和空配置。偏好更新使用串行写入与文件锁。
- 显式选定的离线实例不会回退到其他 daemon；工作区按独立配置和发现目录启动，启动合并和退避按目标隔离。
- 切换关闭旧客户端，刷新各类数据并丢弃旧请求/历史结果；发现信息增加明确运行方式与可执行路径，候选地址限制为回环。
- 验证通过：Go 全量测试、vet、Linux race；Dart analyze 和 Flutter 42 项测试。覆盖并发创建、损坏文件保护、固定实例、启动参数、迟到响应隔离和选择/创建界面。
- 提交：`7885322`（`feat: 增加多实例选择与独立工作区`）。

### 第 7 项：版本检查与升级

- 设置页支持稳定/预发布渠道、平台产物选择、下载与 SHA256 校验、打开下载目录及升级确认；检查使用随客户端附带的工具，不依赖选中的实例。GitHub 仓库随发布构建注入，安装器排除便携标识。
- 外部助手先等待客户端退出，再通过鉴权接口停止同一安装的用户实例，备份并替换程序目录。新版本需完成启动握手；替换或启动失败时恢复原目录，并按原配置重启实例。配置、工作区与私钥保持原位。
- 校验覆盖版本/仓库/平台、归档路径、链接、大小、暂存内容变化与目录边界。便携 daemon 持有共享使用锁，系统服务或未登记实例仍在运行时拒绝替换；回滚也重新检查目录占用。只读安装仍可运行，但不能自动替换。
- 发布工作流写入便携标识和实际仓库；macOS 直接分发包关闭 App Sandbox 以支持外部助手和用户工作区，保留 ad-hoc 签名。新增 Windows/macOS 原生升级夹具 CI。
- 验证通过：Windows Go 全量测试、vet、Linux 全量 race；Dart analyze、Flutter 47 项测试；actionlint、17 项版本解析、Release 发布场景、安装器编译及 14 项自启路径匹配。真实子进程夹具验证等待退出、升级、启动失败回滚、daemon 恢复及配置保留；Windows 发布工具/标识校验和 Linux amd64、macOS amd64/arm64 交叉编译通过。
- 验证边界：当前环境未运行 macOS 原生 GUI/签名打包，由发布 runner 和新增平台 CI 验证；未执行实际 GitHub 发布或替换现有安装。
- 提交消息：`feat: 增加版本检查与可回滚便携升级`。

### 自用体验补充：托盘快捷操作与目标服务诊断（2026-09-13）

- 托盘按当前工作区分组展示隧道，可启停单条或整个分组，并复制连接地址；反向转发明确复制远端监听地址。命令绑定菜单版本与 daemon 实例，切换实例后旧菜单失效；部分失败会提示具体隧道。
- 隧道卡片增加「诊断连接」，通过鉴权接口检查已保存配置的监听端口、SSH 认证与跳板链、目标 TCP 服务，显示各项耗时与原因并支持复制报告。诊断使用独立连接，不改变隧道运行状态或配置；取消、超时和实例切换会释放诊断连接。
- 本地转发通过 SSH 探测目标，反向转发探测本机目标。SOCKS5 没有固定目标、已停止的反向隧道没有运行中的监听，这些项目明确标记为未检查。诊断只验证 TCP 可达性，不验证业务账号或协议。
- 验证通过：Windows Go 全量测试、vet、Dart analyze、Flutter 55 项测试。新增回归覆盖真实 SSH 目标探测、端口冲突、目标不可达、运行中隧道保护、取消清理、API 鉴权、菜单旧事件隔离、批量部分失败及诊断界面交互。
- 验证边界：本机未启用 CGO，`go test -race` 未执行；托盘菜单和动作通过方法通道测试验证，未进行各平台原生桌面的手动交互验证。SDK 目录保持只读。

### 自用体验补充：休眠与换网恢复（2026-09-13）

- daemon 监听 Windows 唤醒、IP 地址与路由变化，并用跨平台网卡轮询和墙上时钟间断检测兜底。事件合并后并发探测现有连接，健康 SSH 会话保持原状；失效连接、拨号与长退避等待会切换到新一轮连接。
- `desired_running` 区分用户的启动意图与运行循环。重试耗尽后保留恢复意图，网络变化可重新尝试；手动停止、批量停止、配置移除和关闭 daemon 会取消意图。恢复先释放旧资源，期间手动停止可以取消尚未开始的新连接。
- 客户端卡片与托盘提供「停止自动恢复」。连续失败 3 次或重试耗尽仅通知一次，恢复后再通知一次；故障计数处理重试归零和工作区切换。通知采用 `local_notifier`，Linux 发布依赖补充 libnotify。
- 验证通过：Windows Go 全量测试与 vet、Dart analyze、Flutter 59 项测试、Windows release 构建。覆盖唤醒检测、健康连接保护、长退避唤醒、耗尽重试后的恢复、手动停止竞争、原生监听释放、通知去重及失效通知丢弃。
- 验证边界：未实际休眠机器或切断当前网络，macOS/Linux 原生桌面通知尚未手工验证；本机未启用 CGO，race 检查仍需在 CI 执行。SDK 目录保持只读，未替换正在运行的安装。

### 自用体验补充：常用连接便利功能（2026-09-13）

- 隧道支持收藏置顶、仅看收藏、上移/下移固定排序；托盘增加按相同顺序排列的收藏入口。偏好按 daemon 配置路径隔离，端口、令牌和进程变化不影响偏好；编辑器改名同步迁移，避免继承已删除同名项的打开方式。
- 隧道页支持 Ctrl/⌘+F 搜索、Ctrl/⌘+N 新建、Ctrl/⌘+Enter 启停选择或唯一搜索结果，以及 Ctrl/⌘+Alt+1～9 启停前九条收藏。其他页面不会响应这些快捷键。
- 本地转发可配置 http/https 网址或本机程序。地址与参数支持 `{host}`、`{port}`，程序参数逐项传递且不通过 shell。打开前启动并等待连接就绪，使用最终监听端口；重复操作合并，实例切换、手动停止和超时不再启动外部应用。
- 收藏、顺序和打开方式采用独立 JSON 文件、串行写入、文件锁与原子替换；损坏或超大的文件会报错并保留原内容。新增 `url_launcher` 提供平台默认浏览器打开能力。
- 验证通过：Dart analyze、Flutter 78 项测试、Windows release 构建。覆盖并发持久化、工作区隔离、排序与改名迁移、偏好损坏保护、快捷键与配置界面、网址校验、原样参数传递、连接就绪等待、重复打开合并及迟到结果隔离。
- 验证边界：外部打开使用临时程序夹具与注入的启动器验证，未启动真实数据库客户端；未替换当前安装或修改 SDK 目录。Go 代码沿用上一项已通过的全量测试与 vet 结果。
